# Lombok configuration — copy @Qualifier to constructor parameters
# so that @RequiredArgsConstructor correctly propagates Spring qualifiers.
config.stopBubbling = true
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
